# Repo-wide Python environment (install once via `pip install -r requirements.txt`)

# Common API clients & helpers
python-dotenv==1.0.1
openai>=1.0.0,<2.0.0
langfuse>=2.0.0,<3.0.0

# Local SLM + fine-tuning stack
torch>=2.0.0
transformers>=4.40.0,<5.0.0
accelerate>=0.25.0,<1.0.0
bitsandbytes>=0.42.0
peft>=0.11.0,<1.0.0
unsloth
datasets>=2.19.0,<4.0.0
trl>=0.9.6,<1.0.0
